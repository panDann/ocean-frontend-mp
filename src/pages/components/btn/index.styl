
@import '~@src/styles/variables'

.btn {
    margin: 0.2rem 1rem;
    padding: 0.4rem;
    color: #fff;
    font-size: 30rpx;
    letter-spacing: 2px
    border-radius: 7rpx;
    text-align: center;

    }
.btn:active{
    opacity .7
    }
.primary-btn {
    background: $primaryColor
    }